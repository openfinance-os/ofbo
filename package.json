{
  "name": "ofbo",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@9.15.0",
  "overridesNote": "pnpm.overrides below are Q4 dependency-scan floors: HIGH advisories in SHIPPED (production) deps that a PARENT pins below the patched version, so `pnpm update` alone cannot lift them. sharp/postcss are pinned by next (^0.34.3 and an exact 8.4.31); fast-uri is pinned via ajv under @modelcontextprotocol/sdk. A direct dependency is bumped in its own package.json instead of here. Drop an entry once the parent ships the patched range.",
  "pnpm": {
    "overrides": {
      "sharp": "^0.35.0",
      "postcss": "^8.5.18",
      "nanoid@3": "^3.3.17",
      "fast-uri": "^3.1.5",
      "ip-address": "^10.3.1",
      "brace-expansion@1": "^1.1.18",
      "brace-expansion@2": "^2.1.4",
      "brace-expansion@5": "^5.0.9",
      "js-yaml": "^4.3.0",
      "undici": "^7.29.0",
      "vite": "^6.4.3",
      "ws": "^8.21.0"
    },
    "overridesNote": "pnpm.overrides are dependency-scan floors: advisories whose PARENT pins the package below the patched version, so `pnpm update` alone cannot lift them. A direct dependency is bumped in its own package.json instead of here. Drop an entry once the parent ships the patched range. PRODUCTION floors (gate Q4 `pnpm audit --prod --audit-level=high`): sharp/postcss are pinned by next (^0.34.3 and an exact 8.4.31); nanoid is pinned by postcss below the GHSA-2v37-7h3g-55p8 patch (next > postcss > nanoid@3.3.16, patched >=3.3.17) — scoped to @3 so a future 5.x consumer is not dragged back to the CJS-era 3.x line; fast-uri (via ajv) and ip-address (via express-rate-limit) are pinned under @modelcontextprotocol/sdk. DEV-TREE floors (HARNESS-12 \u2014 not gated by Q4, which is --prod, but these tools run in CI holding a token, so supply-chain risk is real): brace-expansion is ranged per major because three incompatible lines are in the tree at once (minimatch 3/9/10); js-yaml via eslint + @redocly; undici via wrangler/miniflare; vite via the stryker vitest-runner path."
  },
  "scripts": {
    "gen": "pnpm --filter @ofbo/contracts gen",
    "build": "pnpm -r --if-present build",
    "lint": "eslint .",
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "vitest run --project unit",
    "test:integration": "vitest run --project integration",
    "test:smoke": "vitest run --project smoke",
    "test:integrity": "node scripts/test-integrity.mjs",
    "verify:contract": "pnpm --filter @ofbo/bff verify:contract",
    "docs:check": "node scripts/doc-link-check.mjs",
    "discovery:link": "node scripts/discovery-link-check.mjs",
    "test:mutation": "stryker run",
    "db:apply": "pnpm --filter @ofbo/db apply",
    "db:seed": "pnpm --filter @ofbo/db seed",
    "db:reset": "pnpm --filter @ofbo/db reset",
    "test:coverage": "vitest run --project unit --coverage",
    "test:coverage:full": "vitest run --project unit --project integration --coverage --coverage.include='services/bff/src/**' --coverage.include='packages/db/src/**' --coverage.include='packages/contracts/src/**' --coverage.include='packages/synthetic-data/src/**' --coverage.include='packages/redaction/src/**' --coverage.include='packages/release-evidence/src/**' --coverage.thresholds.lines=0 --coverage.thresholds.statements=0 --coverage.thresholds.functions=0 --coverage.thresholds.branches=0",
    "db:seed:demo": "pnpm --filter @ofbo/db seed:demo",
    "db:seed:tenants": "pnpm --filter @ofbo/db seed:tenants",
    "demo:fault": "bash scripts/demo-fault.sh",
    "demo:break": "pnpm --filter @ofbo/bff demo:break",
    "demo:ingest": "pnpm --filter @ofbo/bff demo:ingest"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@stryker-mutator/core": "^9.6.1",
    "@stryker-mutator/vitest-runner": "^9.6.1",
    "@types/node": "^22",
    "@types/pg": "^8.11.10",
    "@vitejs/plugin-react": "^4.3.4",
    "@vitest/coverage-v8": "^3.2.7",
    "eslint": "^9.17.0",
    "jsdom": "^30.0.1",
    "pg": "^8.13.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.0",
    "vitest": "^3.2.7"
  }
}
